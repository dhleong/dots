#!/bin/bash

path="."
if [ $# -ge 1 ]
then
    path=$1
fi

(
    (cd $path && git ls-tree -r --name-only HEAD) \
        || find $path -path "*/\.*" -prune -o -type f -print -o -type l -print | sed s/^..//
) \
    | ag -v vendor/
